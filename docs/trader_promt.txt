使用python开发语言，针对中国期货市场，生成完整的量化交易程序
1 包含数据获取、回测和交易
2 模型和策略部分由另外一个项目实现，本项目只需要接收那个项目的推荐品种数据即可

<add1>
为了更真实模拟场景，需要进行分钟级别的回测，同时需要分钟级别的数据

<add2>
需要根据给定的品种列表，以及开始结束日期，循环并获取所有的分钟级别以及日级别的数据，获取数据后存储到csv。
并且下次再次获取时，实现增量获取：即能够根据已经存储的数据，自动调整开始和结束日期，避免重复调用api。


<add3>
只给出品种列表如焦煤(JM),玻璃(FG).程序自行判断每个品种在每日对应的主力合约和次主力合约，并拉取对应合约的所有数据.
同样支持给定开始结束日期，循环获取，以及存储，以及增量获取

